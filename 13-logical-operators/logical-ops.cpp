#include <iostream>
using namespace std

; int main () {
    bool a {true}
    ; bool b {false}
    ; bool c {true}
    ; cout << boolalpha
    // AND
    ; cout << "a && b: " << (a && b) << endl
    ; cout << "a && c: " << (a && c) << endl

    ; return 0 ;
}