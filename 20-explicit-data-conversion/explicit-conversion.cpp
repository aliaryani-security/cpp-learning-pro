#include <iostream>
using namespace std

; int main () {
    /*
    explicit conversions are supposed to 
    be done by you, the developer.
    we do this using `static_cast<T>(var)`
    */
    

    ; return 0 ;
}